@import { config as defaultConfig } from './config.jess';
@let config: $defaultConfig;

html {
  box-sizing: border-box;
}

*, *::before, *::after {
  box-sizing: inherit;
}

html, body {
	position: relative;
	width: 100%;
	height: 100%;
}